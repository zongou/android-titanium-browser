name: Ccache Build

on:
  push:
    paths: .github/workflows/cached_build.yml
  workflow_dispatch:
    inputs:
      timeout:
        description: "Timeout in minutes"
        required: false
        type: number
        default: 350

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build0:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/ccache_build
        with:
          timeout: ${{ inputs.timeout }}

  build1:
    needs: build0
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/ccache_build
        with:
          timeout: ${{ inputs.timeout }}

  build2:
    needs: build1
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/ccache_build
        with:
          timeout: ${{ inputs.timeout }}
